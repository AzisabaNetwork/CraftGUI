# CraftGUI Configuration
configVersion: 1.1

# Prefixの設定
# メッセージの先頭に付与されます
prefix: "&7[&aCraftGUI&7] &r"

# 外部ファイルを参照したリロード時の参照先URL
configUrl: "URL"

# CraftGUIのレシピを自動でリロードする間隔（分単位）
# 0または設定しない場合は無効になります
auto-reload-interval-minutes: 60